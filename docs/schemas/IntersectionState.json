{
  "description": "One control-step snapshot of the intersection, ready to feed the DQN.",
  "properties": {
    "intersection_id": {
      "description": "Unique ID for this intersection (e.g., 'hcm.q1.nvc-lelai').",
      "title": "Intersection Id",
      "type": "string"
    },
    "site_id": {
      "description": "Cluster or region ID used for routing.",
      "title": "Site Id",
      "type": "string"
    },
    "schema_version": {
      "default": 1,
      "description": "Version of the event schema.",
      "title": "Schema Version",
      "type": "integer"
    },
    "event_ts": {
      "description": "Timestamp when the event was produced at the source.",
      "format": "date-time",
      "title": "Event Ts",
      "type": "string"
    },
    "ingest_ts": {
      "anyOf": [
        {
          "format": "date-time",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Timestamp when the bus received it. Measures end-to-end latency.",
      "title": "Ingest Ts"
    },
    "step": {
      "description": "Simulation step this state was captured at.",
      "minimum": 0,
      "title": "Step",
      "type": "integer"
    },
    "grid": {
      "description": "Length-80 occupancy vector. Index layout: 4 approaches (East, North, West, South - common.constants.Direction order) x 20 cells each, cell 0 nearest the stop line. 1.0 = at least one vehicle occupies that 5m cell, 0.0 = empty.",
      "items": {
        "type": "number"
      },
      "title": "Grid",
      "type": "array"
    },
    "total_waiting_time_s": {
      "description": "Sum of getWaitingTime() across every vehicle in the network right now.",
      "minimum": 0.0,
      "title": "Total Waiting Time S",
      "type": "number"
    }
  },
  "required": [
    "step",
    "grid",
    "total_waiting_time_s"
  ],
  "title": "IntersectionState",
  "type": "object"
}
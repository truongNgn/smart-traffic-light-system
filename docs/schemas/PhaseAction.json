{
  "$defs": {
    "PhaseAction": {
      "description": "Traffic-police-style green phases: both opposite approaches run together.",
      "enum": [
        0,
        1
      ],
      "title": "PhaseAction",
      "type": "integer"
    }
  },
  "description": "Command sent by the AI agent to the executor.\n\n`target_phase` is the canonical model/control contract. `target_direction`\nis accepted for older mock clients and mapped to that direction's two-way\nphase.",
  "properties": {
    "intersection_id": {
      "description": "Unique ID for this intersection (e.g., 'hcm.q1.nvc-lelai').",
      "title": "Intersection Id",
      "type": "string"
    },
    "site_id": {
      "description": "Cluster or region ID used for routing.",
      "title": "Site Id",
      "type": "string"
    },
    "schema_version": {
      "default": 1,
      "description": "Version of the event schema.",
      "title": "Schema Version",
      "type": "integer"
    },
    "event_ts": {
      "description": "Timestamp when the event was produced at the source.",
      "format": "date-time",
      "title": "Event Ts",
      "type": "string"
    },
    "ingest_ts": {
      "anyOf": [
        {
          "format": "date-time",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Timestamp when the bus received it. Measures end-to-end latency.",
      "title": "Ingest Ts"
    },
    "target_phase": {
      "$ref": "#/$defs/PhaseAction",
      "description": "The two-way green phase to grant."
    }
  },
  "required": [
    "target_phase"
  ],
  "title": "PhaseAction",
  "type": "object"
}
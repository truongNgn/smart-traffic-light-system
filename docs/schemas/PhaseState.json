{
  "$defs": {
    "Direction": {
      "description": "The four physical approaches represented by the 80-cell state.",
      "enum": [
        0,
        1,
        2,
        3
      ],
      "title": "Direction",
      "type": "integer"
    },
    "PhaseAction": {
      "description": "Traffic-police-style green phases: both opposite approaches run together.",
      "enum": [
        0,
        1
      ],
      "title": "PhaseAction",
      "type": "integer"
    }
  },
  "description": "The current physical state of the intersection.",
  "properties": {
    "intersection_id": {
      "description": "Unique ID for this intersection (e.g., 'hcm.q1.nvc-lelai').",
      "title": "Intersection Id",
      "type": "string"
    },
    "site_id": {
      "description": "Cluster or region ID used for routing.",
      "title": "Site Id",
      "type": "string"
    },
    "schema_version": {
      "default": 1,
      "description": "Version of the event schema.",
      "title": "Schema Version",
      "type": "integer"
    },
    "event_ts": {
      "description": "Timestamp when the event was produced at the source.",
      "format": "date-time",
      "title": "Event Ts",
      "type": "string"
    },
    "ingest_ts": {
      "anyOf": [
        {
          "format": "date-time",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Timestamp when the bus received it. Measures end-to-end latency.",
      "title": "Ingest Ts"
    },
    "active_phase": {
      "anyOf": [
        {
          "$ref": "#/$defs/PhaseAction"
        },
        {
          "type": "null"
        }
      ],
      "description": "Which two-way phase currently has right of way. None if all-red."
    },
    "active_directions": {
      "description": "Physical approaches currently receiving green/yellow.",
      "items": {
        "$ref": "#/$defs/Direction"
      },
      "title": "Active Directions",
      "type": "array"
    },
    "active_direction": {
      "anyOf": [
        {
          "$ref": "#/$defs/Direction"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Legacy single-direction field; first active direction when present."
    },
    "is_yellow": {
      "default": false,
      "description": "True if the active phase is currently showing yellow.",
      "title": "Is Yellow",
      "type": "boolean"
    },
    "is_all_red": {
      "default": false,
      "description": "True if all lights are currently red (safety buffer).",
      "title": "Is All Red",
      "type": "boolean"
    }
  },
  "required": [
    "active_phase"
  ],
  "title": "PhaseState",
  "type": "object"
}
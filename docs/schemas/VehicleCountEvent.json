{
  "description": "The enriched event emitted by the vision pipeline for one frame.",
  "properties": {
    "intersection_id": {
      "description": "Unique ID for this intersection (e.g., 'hcm.q1.nvc-lelai').",
      "title": "Intersection Id",
      "type": "string"
    },
    "site_id": {
      "description": "Cluster or region ID used for routing.",
      "title": "Site Id",
      "type": "string"
    },
    "schema_version": {
      "default": 1,
      "description": "Version of the event schema.",
      "title": "Schema Version",
      "type": "integer"
    },
    "event_ts": {
      "description": "Timestamp when the event was produced at the source.",
      "format": "date-time",
      "title": "Event Ts",
      "type": "string"
    },
    "ingest_ts": {
      "anyOf": [
        {
          "format": "date-time",
          "type": "string"
        },
        {
          "type": "null"
        }
      ],
      "default": null,
      "description": "Timestamp when the bus received it. Measures end-to-end latency.",
      "title": "Ingest Ts"
    },
    "camera_id": {
      "description": "Originating camera ID.",
      "title": "Camera Id",
      "type": "string"
    },
    "class_counts": {
      "additionalProperties": {
        "type": "integer"
      },
      "description": "Dictionary mapping vehicle class (e.g., 'car', 'bus') to count.",
      "title": "Class Counts",
      "type": "object"
    },
    "lane_counts": {
      "additionalProperties": {
        "type": "integer"
      },
      "description": "Dictionary mapping lane ID (e.g., 'E-Left', 'E-Straight') to current count.",
      "title": "Lane Counts",
      "type": "object"
    },
    "mean_speed_mps": {
      "default": 0.0,
      "description": "Estimated mean speed of all tracked vehicles in meters per second.",
      "title": "Mean Speed Mps",
      "type": "number"
    },
    "centroids": {
      "additionalProperties": {
        "maxItems": 2,
        "minItems": 2,
        "prefixItems": [
          {
            "type": "number"
          },
          {
            "type": "number"
          }
        ],
        "type": "array"
      },
      "description": "Dictionary mapping track ID to (x, y) centroid coordinates.",
      "title": "Centroids",
      "type": "object"
    }
  },
  "required": [
    "camera_id"
  ],
  "title": "VehicleCountEvent",
  "type": "object"
}